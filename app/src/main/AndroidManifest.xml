<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vsewa">
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoInternetTheme">
        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="face" />
        <activity
            android:name=".NavigationButton.BottomNavigatioActivity"
            android:label="title_activity_bottom_navigation"></activity>
        <activity android:name=".Authentication.RegisterWithEmailActivity" />
        <activity android:name=".Authentication.ForgotPassword" />
        <activity android:name=".Authentication.LoginWithEmailIdNeedy" />
        <activity android:name=".Authentication.LoginWithEmailIdVolunteer" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Welcome screen activity start first and then Main Activity Starts -->
        <activity android:name=".WelcomeScreen" />
    </application>

</manifest>